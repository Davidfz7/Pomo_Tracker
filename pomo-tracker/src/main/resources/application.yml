spring:
    datasource:
        url: jdbc:mysql://localhost:3306/PomoDB?allowPublicKeyRetrieval=true&useSSL=false
        username: david
        password: david123
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        hibernate:
            ddl-auto: validate
        show-sql: false
        properties:
            hibernate:
                format_sql: true
        database: mysql
        database-platform: org.hibernate.dialect.MySQLDialect